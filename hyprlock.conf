# --- переменные (под себя) ---
$font      = JetBrainsMono Nerd Font
$accent    = rgba(120, 180, 255, 1.0)
# популярные места для аватарки: ~/.face или /var/lib/AccountsService/icons/<юзер>

general {
  disable_loading_bar = true
  hide_cursor = true
  grace = 0
}

# --- фон: картинка + затемняющая вуаль ---
background {
  monitor =
  path = screenshot
  color = rgba(15, 15, 20, 1.0)
  blur_passes = 3   # 0 = без размытия
  blur_size = 8
}

# мягкое затемнение поверх картинки (чтобы текст читался)
shape {
  monitor =
  size = 100%, 100%
  color = rgba(0, 0, 0, 0.35)
  rounding = 0
  zindex = 0
}

# --- поле ввода пароля ---
input-field {
  monitor =
  size = 360, 64
  rounding = 18
  outline_thickness = 2
  outer_color = rgba(255,255,255,0.14)
  inner_color = rgba(0,0,0,0.45)
  font_color  = rgba(235,238,245,1.0)
  fade_on_empty = false

  placeholder_text = <span font_desc="$font 18">  $USER — $LAYOUT</span>
  check_color = rgba(120,200,120,0.90)
  fail_color  = rgba(220,80,80,0.95)
  fail_text   = <span font_desc="$font 14"><b>Неверно</b>  ($ATTEMPTS)</span>

  position = 0, 30
  halign = center
  valign = center
  zindex = 2
}

# --- часы (крупно, сверху справа) ---
label {
  monitor =
  text = cmd[update:1000] date +"%H:%M"
  color = rgba(245,247,250,1.0)
  font_size = 120
  font_family = $font
  position = -60, 40
  halign = right
  valign = top
  shadow_passes = 1
  shadow_size = 6
  shadow_color = rgba(0,0,0,0.6)
}

# --- дата (под часами) ---
label {
  monitor =
  text = cmd[update:60000] date +"%A, %d %B"
  color = rgba(220,225,233,0.9)
  font_size = 22
  font_family = $font
  position = -60, 170
  halign = right
  valign = top
}

# --- батарея (снизу слева) ---
label {
  monitor =
  text = cmd[update:30000] bash -lc 'c=$(cat /sys/class/power_supply/BAT*/capacity 2>/dev/null | head -n1); echo "🔋 ${c:-?}%";'
  color = rgba(220,225,233,0.9)
  font_size = 16
  font_family = $font
  position = 20, -20
  halign = left
  valign = bottom
}
